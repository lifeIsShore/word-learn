// ─────────────────────────────────────────────────────────────────────────────
//  firebase_options.dart — COMPILE-SAFE STUB (no Firebase dependency)
//
//  firebase_core is commented out in pubspec.yaml until google-services.json
//  is configured. This stub replaces the real FirebaseOptions class so the
//  project compiles without the firebase_core package.
//
//  HOW TO RESTORE FIREBASE:
//  1. Create a Firebase project at https://console.firebase.google.com
//  2. Add Android app (package: com.wordlearn.word_learn)
//  3. Add iOS app (bundle ID: com.wordlearn.wordLearn)
//  4. Run: dart pub global activate flutterfire_cli && flutterfire configure
//  5. Uncomment firebase_core + firebase_messaging in pubspec.yaml
//  6. Set AppConfig.devModeSkipFirebase = false
//  7. Delete this stub — the real firebase_options.dart will replace it
// ─────────────────────────────────────────────────────────────────────────────

// No imports needed — Firebase packages are not active.
// This file is intentionally empty so that any accidental import of
// DefaultFirebaseOptions fails loudly at the call site rather than here.
